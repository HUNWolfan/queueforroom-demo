name = "queueforroom"
compatibility_date = "2024-11-04"
pages_build_output_dir = "./build/client"

[env.production]
vars = { NODE_ENV = "production" }

# Database binding will be configured in Cloudflare dashboard
# [env.production.d1_databases]
# DATABASE = { binding = "DB", database_name = "queueforroom-db", database_id = "your-database-id" }
